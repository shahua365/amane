# NAS FC2 验收 (2026-09-16)

## 版本与隔离

代码 `ee5be5245a2ae592024112a1a81490487ea75e59` 完成三个样本测试; Cookie 迁移与排队冷却修复 `3bbdc5a502063e320437f20ec929e167b4699b9a` 单独复验. 后续交接文档提交不改变镜像代码.

生产 `Amane` 保留 v0.11.0 镜像 `amane:age-detection-20260913`, 未重启或升级. 候选为 v0.14.0, 镜像 `amane:fc2-multisource-3bbdc5a`. 没有挂载用户媒体; Dockerfile 自建的 /media 是空匿名 volume. 只运行独立刮削脚本, 没有启动 worker、调度或 organize. 生产配置以只读方式加载, 测试数据库与资源在独立目录.

生产数据目录下 `backups/fc2-multisource-20260916` 保存 SQLite 在线备份和配置; 数据库 quick_check 为 ok. Compose 与环境文件备份在项目 `backups/fc2-multisource-20260916`, 权限限制为 0700/0600. 备份和原始运行数据均不进入 Git. 未对生产库执行迁移.

## 首轮样本

| number | 有效来源 | metadata | poster | extrafanart | 无网络复用 poster |
|---|---|---|---|---|---|
| FC2-PPV-4974556 | fc2 / javarchive / fc2cmadb | 标题、日期、seller、114 分钟、22 个去重标签 | 已缓存 | 已缓存, 每源最多抽取 3 个 URL | 成功 |
| FC2-PPV-4975102 | fc2 / fc2cmadb | 标题、日期、seller、125 分钟、10 个标签 | 已缓存 | 2 个 URL 已缓存 | 成功 |
| FC2-PPV-3193265 | javarchive | 标题; 日期、seller、runtime 缺失 | 已缓存 | 源未提供 | 成功 |

旧样本官方无可用结果、FC2CMADB 404 时, JavArchive 保留标题和封面; 数据均写入隔离库. FD2PPV 首次 Cloudflare 403 后, 后续样本停止该源. 没有伪造 actress 或缺失字段.

首轮 30 次实际 HTTP: 28 次 200、1 次 403、1 次 404; 无 429、timeout 或图片 404. 404 来自 FC2CMADB 的旧样本. HTML 无结果与解析失败不能完全区分, 不据此宣称所有页面兼容.

| 实际请求 host | 请求数 | 配置 req/s | 观察到最短间隔秒 |
|---|---:|---:|---:|
| adult.contents.fc2.com | 3 | 0.3 | 87.067 |
| javarchive.com | 5 | 0.3 | 3.334 |
| fc2cmadb.com | 5 | 0.02 | 50.000 |
| fd2ppv.cc | 1 | 0.05 | 单次无法衡量 |
| contents-thumbnail2.fc2.com | 14 | 0.5 | 2.000 |
| img.javstore.net | 2 | 0.5 | 2.001 |

未请求 fc2ppvdb. 已配置 Cookie/代理值在捕获日志中的匹配结果为 false; 这只覆盖本次运行已知值, 不是所有潜在敏感格式的证明. 标签 field_sources 仍记录主要来源, 不能表达每个合并标签的独立归属.

## 最终镜像复验

`3bbdc5a` 的 GitHub [CI](https://github.com/shahua365/amane/actions/runs/35064932961) 全部通过. NAS 镜像 ID 前缀 `0cc6ac9d0387`; 隔离容器 `amane-fc2-final-20260916` 为 exited / 0.

复验 FC2-PPV-4974556: 官方、JavArchive、FC2CMADB 均成功; 标题、日期、seller、114 分钟、22 个合并标签保存. 1 个封面和 4 个唯一剧照候选 URL 可从缓存解析 (部分 URL 也可能作为 thumb 获取); 封面重复获取没有网络请求. 共 13 次 HTTP, FC2CMADB 两次请求相隔 50.002 秒; 日志未匹配到已知 Cookie/代理值, 未请求旧域名. 没有重试 FD2PPV.

复验后生产健康接口返回 ok / 0.11.0; config 和 Compose 与备份字节一致, 原镜像继续 running. 本轮没有执行生产升级, 生产版仍需另行评估 v0.11 → v0.14 数据库兼容迁移.

## 验证边界

- VERIFIED: 上述来源在 NAS 的有效样本, metadata 入库、真实图片下载和现有 ResourceStore 无网络复用.
- PARTIAL: 没有运行 organize 或移动媒体; 验证的是其共享资源获取接口. 图片 URL 失效行为由离线测试覆盖, 没有对真实站点制造 404.
- PARTIAL: 沿用现有 extrafanart 首个有图片来源策略, 没有实现跨来源完整剧照合集; 本次仅有限抽样, 不代表所有图片都可下载.
- FAILED: FD2PPV 自动化访问为 Cloudflare 403; 实现已注册, 不等于 live 可用.
- NOT TESTED: MissJAV、Thikana、7MMTV、AV01 未获得可靠低维护接入依据, 未注册 crawler. 非 FC2 公网站点没有逐站 live; 相关回归以 CI 为准.
- PARTIAL: 无分布式限流; 只在同一 Amane 进程的 worker 之间共享. 404 负缓存仅进程内 12 小时.

## 回滚

生产仍是部署前版本, 无需恢复数据库或 Compose. 若隔离任务仍在运行, 在 NAS 执行以下命令即可停止候选; 已退出时无需操作:

```sh
sudo -n /usr/local/bin/docker-for-8anana stop amane-fc2-final-20260916
```

保留原生产镜像、备份、隔离数据库与缓存, 不删除任何媒体或数据.
